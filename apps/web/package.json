{
  "name": "@full-shelf/web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Full Shelf v4 operator presentation (React + TypeScript + Vite). Vendored Design handoff; no live transport yet.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "echo \"no test runner configured for apps/web yet\" && exit 0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
