node_modules
dist
dist-ssr
*.local
tsconfig.tsbuildinfo

# Playwright: browser binaries live in the shared cache, never in Git.
test-results
playwright-report
.playwright

# Browser Maps key is supplied at runtime and never committed.
.env
.env.local
