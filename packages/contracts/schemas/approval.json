{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FullShelfApprovalEnvelope",
  "type": "object",
  "properties": {
    "approval_id": { "type": "string" },
    "rev_id": { "type": "string" },
    "principal_id": { "type": "string" },
    "incident_id": { "type": "string" },
    "plan_id": { "type": "string" },
    "source_revision": { "type": "string" },
    "proposed_revision": { "type": "string" },
    "plan_diff": { "type": "object" },
    "kms_key_version": { "type": "string" },
    "kms_signature": { "type": "string" },
    "expires_at": { "type": "string", "format": "date-time" }
  },
  "required": [
    "approval_id", "rev_id", "principal_id", "incident_id",
    "plan_id", "source_revision", "proposed_revision", "plan_diff",
    "kms_key_version", "kms_signature", "expires_at"
  ]
}
